---
AIGC:
    Label: "1"
    ContentProducer: 001191110102MACQD9K64018705
    ProduceID: 2367715123927392_0-data_volume/7662265703512375603-files/所有对话/主对话/codeact/output/游戏测试用例生成技能V6.md
    ReservedCode1: ""
    ContentPropagator: 001191110102MACQD9K64028705
    PropagateID: 2367715123927392#1784691004777
    ReservedCode2: ""
name: test-case-generator
description: 根据游戏系统策划文档（Excel/PDF/Word）按标准化11列模板批量生成结构化测试用例并输出带样式的Excel文件；当用户需要根据策划文档生成测试用例、编写QA测试用例、覆盖功能测试点或生成测试excel文件时使用
dependency:
    python:
        - pandas
        - openpyxl
---

# 游戏测试用例生成技能

## 概述

根据游戏系统策划文档，自动提取功能模块与规则逻辑，按标准化11列模板生成结构化测试用例，输出为带样式的 Excel 文件。

## 工作流程

1. **解析策划文档**：用 pandas/openpyxl 读取 Excel 的所有 sheet，逐 sheet 提取功能模块、规则逻辑、配置表、界面交互等内容
2. **模块划分**：按系统架构划分测试模块，为每个模块分配2-4字母缩写（如兵营=BAR，医院=HSP）
3. **功能点枚举**：列出每个模块下的所有功能点
4. **用例设计**：为每个功能点设计正常/异常/边界场景用例
5. **格式化输出**：用 openpyxl 生成带样式的 Excel 文件

## 用例设计标准格式

### Excel 表结构（11列）

| 列名 | 说明 | 格式要求 |
|------|------|----------|
| 用例ID | 唯一标识 | TC-{模块缩写}-{三位序号}，如 TC-BAR-001 |
| 测试模块 | 功能大模块 | 对应系统的一级模块划分 |
| 功能点 | 具体功能点 | 模块下的二级功能点 |
| 场景类型 | 测试场景分类 | 正常场景 / 异常场景 / 边界场景 |
| 用例标题 | 简明描述 | 一句话概括测试目标，动宾结构 |
| 前置条件 | 执行前提 | 执行该用例前需要满足的条件 |
| 操作步骤 | 具体步骤 | 多步骤用换行分隔，编号列出 |
| 预期结果 | 期望输出 | 与操作步骤对应，多条目用换行分隔 |
| 优先级 | 重要程度 | 高 / 中 / 低 |
| 测试类型 | 测试分类 | 功能测试 / 数值测试 / UI测试 / 性能测试 / 异常测试 / 边界测试 |
| 执行状态 | 执行结果 | 留空待填 |

### 用例ID 编号规则
- 前缀固定为 `TC-`
- 模块缩写：2-4个大写字母，取自模块英文名首字母（如兵营=BAR，医院=HSP，校场=SCH，士兵=SOL，英雄带兵=HERO）
- 序号：三位数字，从 001 开始递增

### 场景类型分类
- **正常场景**：符合业务逻辑的标准流程，包含主干路径和主要分支
- **异常场景**：非法输入、网络异常、资源不足、操作顺序错误、反向操作
- **边界场景**：最大值、最小值、临界值、0值、空值

### 测试类型分类
- **功能测试**：验证功能逻辑正确性
- **数值测试**：验证数值计算、概率分布、权重随机
- **UI测试**：验证界面显示、布局、文案、弹窗
- **性能测试**：验证大量数据、并发等场景
- **异常测试**：验证异常处理、容错能力
- **边界测试**：验证边界值、极限条件

### 优先级定义
- **高**：核心功能、主流程、高风险点
- **中**：重要分支、常见操作、一般功能
- **低**：边缘场景、低频操作、次要功能

## 用例设计原则

### 完整覆盖
- 覆盖策划文档中的所有模块和功能点
- 每个功能点至少包含正常场景用例
- 关键功能点需覆盖异常和边界场景

### 去重校验规则（强制）
- 生成用例后必须执行去重检查，去除高度相似或完全重复的用例
- 去重判定标准：用例标题相同 或 前置条件+操作步骤+预期结果三者同时高度相似（相似度>80%）
- 去重后保留覆盖更全面的版本，或合并为一条更完整的用例
- 最终输出的用例列表中不允许存在任何重复

### 正常场景要求
- 操作流程符合策划文档的标准流程
- 使用正常范围内的合法参数和数据
- 包含主干路径和主要分支选择路径
- 前端显示界面、文案、布局符合设计规范

### 异常场景要求
- 非法输入：格式错误、内容违规、特殊字符
- 边界值测试：最大值、最小值、临界值、0值、空值
- 唯一性校验：字段重复值、唯一约束验证
- 操作顺序错误：反向操作、跳过关键步骤、越级访问

### 数值专项要求
- 概率分布验证（如权重、随机）
- 资源消耗与产出数值一致性
- 等级/品质数值梯度正确性
- 计算公式正确性验证

### 时间相关要求
- 倒计时准确性和状态变更
- 压后台/断线重连后的时间同步
- 跨天/跨周期重置逻辑
- 修改系统时间防作弊

## 系统专项测试维度（所有系统必测）

> 以下6个维度适用于所有游戏系统，不论系统类型都必须覆盖。根据各系统实际情况，判断每个维度的适用场景后设计用例。

### 维度1：多玩家状态矩阵（关键数据节点）

系统关键数据节点（如奖励发放、数据结算、状态变更、周期重置、开启/结束）必须按玩家状态组合展开测试：

**玩家参与维度 × 在线状态维度：**
- 未参与/未触达玩家 × 在线等待状态变更
- 未参与/未触达玩家 × 离线等待状态变更
- 已参与/已触达玩家 × 在线等待状态变更
- 已参与/已触达玩家 × 离线等待状态变更
- 已参与/已触达玩家 × 在系统界面内等待状态变更（界面内实时状态切换）

**每种组合需验证：**
1. 数据变更是否正确（该发的发、不该发的不发）
2. 补发邮件是否完整且正确
3. 状态切换是否实时生效
4. 数据记录是否准确

**跨期/跨轮次专项（适用于有周期/轮次的系统）：**
- 上一期参与的玩家，一直离线，等待下一期开启时：
  - 上一期奖励发放/补发是否正常
  - 数据清除时机是否正确（结算时 or 新一期开启时）
  - 旧数据是否被正确清理，不影响新一期数据
  - 新一期开启后玩家能否正常使用
  - 跨期数据继承（如有）是否正确

### 维度2：账号切换与数据隔离

- 切换账号后，各账号之间的系统数据互不干扰
- 切换账号后，已领取的奖励/已完成的操作互不影响
- 切换账号后，无缓存残留显示（界面不显示上一账号的数据/状态）
- 切换账号后，系统进度/积分/排行榜/配置数据正确刷新
- 频繁切换账号（如连续切换5次）后数据一致性

### 维度3：排行榜容灾处理

当系统包含排行榜功能时（排行榜、榜单、排名等）：
- 异常玩家数据被GM删除后，排行榜排名是否正确重算
- 异常玩家被删除后，其奖励是否不再发放，且不影响其他玩家的奖励
- 异常玩家被删除后，排行榜界面显示是否正常（无空位/错位/序号断裂）
- 异常玩家被删除后，其他玩家的排名变化是否实时生效
- 批量删除异常玩家后排行榜的数据一致性
- 被删除的异常玩家重新登录后，排行榜数据是否正确处理（不重新上榜/显示为已清除）

### 维度4：跨服数据互通

当系统涉及跨服功能时（跨服活动、跨服竞技场、跨服交易等）：
- 多个服之间的系统数据是否正确互通（积分、排名、进度同步）
- 上榜/参与玩家是否确实属于该跨服分组（分组校验）
- 数据记录中的玩家归属服是否正确
- 当跨服分组内的一个服宕机时：
  - 其他跨服分组内的服是否不受影响
  - 宕机服的玩家在恢复后能否继续参与/使用该系统
  - 宕机期间该服玩家的数据是否有丢失
  - 宕机期间的数据（积分/排名/奖励）在恢复后是否正确补齐
- 跨服结算时，宕机服的数据处理（延迟结算/补偿机制）

### 维度5：服务端操作数据免疫

以下服务端操作不应影响玩家的系统数据：

**停服重启：**
- 停服期间进行中的操作倒计时是否正确暂停/恢复
- 停服前已领取的奖励/已完成的操作记录是否保留
- 停服前未完成的进度/队列是否保留

**宕机：**
- 宕机期间玩家的操作（如已提交但未确认的请求）是否正确处理
- 宕机恢复后系统数据完整性

**合服：**
- 合服后两个服的系统数据是否正确合并/保留
- 合服后排行榜是否正确处理（同服玩家合并后的排名）
- 合服后系统进度/积分/配置是否保留

**玩家迁移服：**
- 迁服后玩家在原服的系统数据是否正确转移
- 迁服后新服的系统数据是否正确继承
- 迁服后跨服排行榜/数据是否正确更新

### 维度6：奖励发放专项

**发放时机验证：**
- 触发发放条件时在线玩家的即时奖励发放
- 触发发放条件时离线玩家的奖励补发（邮件）
- 发放瞬间大量玩家同时在线领奖的并发处理
- 发放前后几秒内的边界领奖

**防重复发放：**
- 同一奖励不可重复领取（多次点击、并发请求）
- 网络异常重试后不可重复发奖
- 断线重连后不可重复发奖
- 多次登录不可重复发奖

**补发完整性：**
- 补发邮件内容与实际应发奖励完全一致
- 补发邮件不重复（同一触发条件只发一次）
- 补发邮件时效性（触发后合理时间内发出）

## Excel 输出规范

### 样式要求
- 表头：蓝色背景(#4472C4)、白色加粗字、居中
- 冻结首行
- 自动筛选
- 列宽自适应
- 全单元格加细边框
- 优先级单元格颜色区分：高=红底(#FFC7CE)、中=黄底(#FFEB9C)、低=绿底(#C6EFCE)

### 文件规范
- 文件格式：.xlsx
- 文件名：`{系统名称}测试用例.xlsx`
- Sheet 名称：`{系统名称}测试用例`
- 第一行为表头，从第二行开始填写用例数据

## 代码注意事项

### 读取策划文档
- 用 pandas 读取 xlsx，逐 sheet 解析
- NaN 值用 `str(x) if pd.notna(x) else ''` 处理，不能用 `astype(str)`（会将 NaN 转为字符串 "nan"）

### 生成用例 Excel
- 用 openpyxl 创建，支持样式设置
- Python 脚本中避免在字符串内嵌套中文引号，否则会导致 SyntaxError
- 操作步骤和预期结果中的多条目用 `\n` 分隔

## 使用示例

### 示例1：根据抽卡系统策划文档生成测试用例
- 场景/输入：用户提供抽卡系统的策划文档（Excel格式），要求生成测试用例
- 预期产出：生成包含入口模块、奖池结构、抽卡逻辑、保底机制等模块的测试用例Excel文件
- 关键要点：需覆盖正常抽卡流程、概率验证、保底机制、异常场景（资源不足、网络异常）

### 示例2：根据兵营系统策划文档生成测试用例
- 场景/输入：用户提供兵营系统的策划文档，要求覆盖所有功能点
- 预期产出：生成包含兵种训练、升级、加速、队列管理等模块的测试用例Excel文件
- 关键要点：需关注数值计算（训练时间、资源消耗）、边界值（队列上限、等级上限）

### 示例3：根据城建系统策划文档生成测试用例
- 场景/输入：用户提供城建系统的策划文档，要求生成QA用例
- 预期产出：生成包含建筑建造、升级、资源产出、布局管理等模块的测试用例Excel文件
- 关键要点：需覆盖时间相关测试（倒计时、跨天重置）、资源计算、前置条件校验

## 资源索引
- 参考：见 [references/gacha_test_case_sample.md](references/gacha_test_case_sample.md)（何时读取：需要了解标准用例格式时，参考抽卡系统的完整用例样例）

## 注意事项
- 仅在需要时读取参考，保持上下文简洁。
- 操作脆弱时优先校验结果。
- 充分利用智能体能力，避免为简单任务编写脚本。

## 系统级测试参考要点

> 以下为各类常见游戏系统的核心测试考虑点。生成用例时，根据目标系统所属类型，参考对应章节补充专项测试点。

### 大型养成系统（城建/英雄/装备/科技）

| 编号 | 测试考虑点 | 关注维度 |
|------|-----------|---------|
| SYS-BUILD-01 | 建筑建造时间在下线/断网重连后正常计数 | 计时一致性 |
| SYS-BUILD-02 | 建筑升级前后属性加成正确生效 | 数值校验 |
| SYS-BUILD-03 | 多建造队列并行时资源扣除与时间独立计算 | 并发逻辑 |
| SYS-BUILD-04 | 英雄抽卡概率与公示一致（蒙特卡洛模拟10万次+卡方检验） | 概率合规 |
| SYS-BUILD-05 | 保底机制准确触发、跨池继承逻辑正确 | 保底机制 |
| SYS-BUILD-06 | 装备强化成功率服务端生成、不可客户端篡改 | 安全性 |
| SYS-BUILD-07 | 科技研究树分支解锁前置条件校验 | 逻辑链路 |
| SYS-BUILD-08 | 资源消耗与获得在弱网/杀进程场景下不出现收支不等 | 数据一致性 |
| SYS-BUILD-09 | 养成属性在战斗/排行榜/展示界面的数值一致 | 多端同步 |
| SYS-BUILD-10 | 数值平衡：战斗胜率40-60%区间分布、产出消耗比1.0±0.15 | 生态健康 |

**着重测试点：**
- 建筑建造计时：SLG核心付费点是"买时间"，计时异常直接导致付费漏洞
- 抽卡概率与保底：概率偏差是玩家投诉最高频问题，涉及监管合规
- 强化/升级资源消耗原子性：弱网下资源扣了但强化未生效是高频bug
- 数值平衡模型：胜率偏离40-60%或产出消耗比偏离1.0±0.15将导致生态崩坏

**异常测试点：**
- 断网/杀进程中建筑升级/强化操作 → 资源与状态一致性
- 修改设备时间加速建造/研究 → 服务端必须校验
- 同一资源同时用于多个消耗操作 → 原子性保证
- 卡池切换时保底计数重置或继承错误
- 服务器回档后养成数据回退

### 活动系统（限时/周期/赛季/七日任务）

| 编号 | 测试考虑点 | 关注维度 |
|------|-----------|---------|
| SYS-ACT-01 | 活动开始/结束时间准确性（含时区/跨天） | 时间逻辑 |
| SYS-ACT-02 | 开启条件（开服天数/等级/注册天数/前置任务）正确校验 | 条件校验 |
| SYS-ACT-03 | 奖励领取状态幂等性（不可重复领取） | 数据一致性 |
| SYS-ACT-04 | 红点逻辑正确（有奖励时亮/领完后灭/条件不满足时不亮） | UI状态 |
| SYS-ACT-05 | 在线跨天/离线跨天活动刷新逻辑 | 时间触发 |
| SYS-ACT-06 | 活动结算逻辑（排名/积分/奖励发放） | 结算逻辑 |
| SYS-ACT-07 | 参与次数与获奖次数限制正确执行 | 限次逻辑 |
| SYS-ACT-08 | 道具投放与预期配置一致 | 配置校验 |
| SYS-ACT-09 | 数据存储可靠性（活动进度不丢失） | 持久化 |
| SYS-ACT-10 | 功能交叉场景（活动道具与非活动系统交互） | 耦合性 |

**着重测试点：**
- 活动时间边界：开始前/结束时最易出问题，奖品提前发放或延迟结束导致经济漏洞
- 奖励幂等性：并发条件下两个请求同时读到"未领取"导致双发奖
- 弱网/切后台/杀进程/服务器重启：短期活动QA时间少但异常场景多
- 在线跨天 vs 离线跨天：两者刷新逻辑不同需分别验证

**异常测试点：**
- 活动期间服务器重启后时间/进度/奖励恢复
- 活动进行中热更配置后已领取状态是否保留
- 网络中断时领奖 → 客户端显示成功但服务端未收到
- 跨时区玩家看到的活动时间/倒计时一致性
- 活动倒计时最后一秒完成活动 → 奖励是否正确发放

### 跨服活动（跨服竞技场/跨服战场/跨服排行）

| 编号 | 测试考虑点 | 关注维度 |
|------|-----------|---------|
| SYS-CROSS-01 | 跨服匹配公平性（战力/等级/段位匹配算法） | 匹配逻辑 |
| SYS-CROSS-02 | 跨服数据传输延迟对战斗结果的影响 | 网络同步 |
| SYS-CROSS-03 | 断线重连后能正常回归战斗且不破坏游戏进程 | 断线恢复 |
| SYS-CROSS-04 | 多服务器并发时的服务器稳定性 | 承载能力 |
| SYS-CROSS-05 | 跨服排行榜实时刷新与数据一致性 | 排行一致性 |
| SYS-CROSS-06 | 跨服战斗结算结果在原服同步 | 数据同步 |
| SYS-CROSS-07 | 弱化战损机制正确生效 | 规则校验 |
| SYS-CROSS-08 | 以军团/战队为单位的报名与参战资格校验 | 资格校验 |

**着重测试点：**
- 跨服匹配公平性：实力差距过大导致体验极差、玩家流失
- 断线重连：PvP中断线是玩家最大痛点
- 延迟和服务器稳定性：Lag是PvP玩家头号挫败感来源

**异常测试点：**
- 跨服战斗中服务器崩溃 → 战斗结果处理/是否支持重赛
- 跨服数据同步延迟超5秒 → 玩家状态不一致
- 跨服排行刷分（利用时差或规则差异）
- 跨服活动期间合服 → 玩家身份/排名处理
- 断线后敌方已完成结算 → 重连后结果判定

### 跨服系统（跨服匹配/数据同步/分组）

| 编号 | 测试考虑点 | 关注维度 |
|------|-----------|---------|
| SYS-SYNC-01 | 跨服数据同步延迟与一致性保障 | 数据同步 |
| SYS-SYNC-02 | 服务发现与节点故障自动切换 | 高可用 |
| SYS-SYNC-03 | 合服/数据互通规则（角色保留/重名/帮派上限） | 合服逻辑 |
| SYS-SYNC-04 | 状态同步更新间隔30-100ms + 插值算法有效性 | 帧同步 |
| SYS-SYNC-05 | 心跳机制序列号防重放 + RTT估算 | 通信安全 |
| SYS-SYNC-06 | 断线重连后会话缓存时效性与状态一致性 | 会话管理 |
| SYS-SYNC-07 | 跨服战区分组变化后的数据迁移 | 数据迁移 |
| SYS-SYNC-08 | 接入层/逻辑层/数据层三级容灾 | 架构容灾 |

**着重测试点：**
- 跨服数据同步一致性：同步延迟超5秒导致状态不一致，68%+运营事故源于未预见的分布式故障
- 合服规则执行：涉及角色上限、重名处理、帮派上限、排行榜重置，任一环节出错引发大规模投诉
- 三级容灾（接入层/逻辑层/数据层）：任一节点故障需自动切换

**异常测试点：**
- 跨服节点心跳丢失10%时是否自动切换
- 合服期间同名角色/帮派的数据冲突
- 跨服session失效需重新鉴权
- 数据库主从切换延迟期间写入数据是否丢失
- 网络分区时的脑裂处理

### 大地图/世界地图

| 编号 | 测试考虑点 | 关注维度 |
|------|-----------|---------|
| SYS-MAP-01 | 自由行军实时移动与方向调整的流畅性 | 操作响应 |
| SYS-MAP-02 | 相遇即战机制正确触发 | 战斗触发 |
| SYS-MAP-03 | 地形对行军速度/战斗属性的影响 | 地形系统 |
| SYS-MAP-04 | 多线操作（佯攻/偷袭/集火）指令的执行与撤销 | 多线操作 |
| SYS-MAP-05 | 攻城战多阶段流程（宣战/集结/攻城/占领） | 攻城流程 |
| SYS-MAP-06 | 天气系统对战斗/行军的影响 | 环境系统 |
| SYS-MAP-07 | 大地图多人同屏渲染性能（千人级） | 渲染性能 |
| SYS-MAP-08 | 资源点采集/争夺的并发冲突处理 | 并发逻辑 |
| SYS-MAP-09 | 领地战宣战/免战/耐久恢复机制 | 领地规则 |

**着重测试点：**
- 多人同屏性能：千人级战斗是性能极限场景，人多时闪退/卡死/看不到敌人
- 自由行军操作响应：操作失灵严重影响战斗策略
- 攻城战多阶段流程：任一环节数据不一致导致战局争议

**异常测试点：**
- 弱网下行军指令延迟/丢失 → 部队卡死
- 数百人同时攻城 → 服务器承载+客户端渲染
- 行军中杀进程重连 → 部队位置/状态正确性
- 两人同时到达资源点 → 优先级判定
- 免战/耐久恢复机制理解不一致导致不公平

### 联盟系统

| 编号 | 测试考虑点 | 关注维度 |
|------|-----------|---------|
| SYS-ALL-01 | 联盟创建条件（资源/等级/冷却时间）校验 | 创建逻辑 |
| SYS-ALL-02 | 成员上限随等级/城池占领扩展的正确性 | 容量扩展 |
| SYS-ALL-03 | 权限体系（会长/副会长/指挥/成员）的继承与变更 | 权限管理 |
| SYS-ALL-04 | 会长不活跃时自动转让逻辑 | 会长继承 |
| SYS-ALL-05 | 联盟科技研发的贡献消耗与属性加成 | 科技系统 |
| SYS-ALL-06 | 联盟商店物品与贡献度兑换的正确性 | 商店系统 |
| SYS-ALL-07 | 联盟解散/退出后个人贡献/奖励的处理 | 退出逻辑 |
| SYS-ALL-08 | 联盟集结/援助的发起与参与逻辑 | 战争系统 |
| SYS-ALL-09 | 联盟邮件/公告的发送与接收 | 通信系统 |
| SYS-ALL-10 | 联盟成就/礼物发放的条件与范围 | 奖励系统 |

**着重测试点：**
- 权限体系：边界不清可能导致低权限执行高权限操作（踢人/解散），造成严重社交事故
- 会长自动转让：逻辑错误可能导致联盟被"夺权"
- 退出/解散后数据归属：贡献度/商店兑换/成就奖励处理最易引发纠纷

**异常测试点：**
- 最后一人退出时联盟是否自动解散
- 升级和占领同时增加上限时是否正确叠加
- 联盟科技研发中退出 → 已投入贡献是否退还
- 跨服期间联盟操作是否受限
- 多人同时申请加入/退出时的状态一致性

### GVG玩法（公会战/联盟战/领地争夺战）

| 编号 | 测试考虑点 | 关注维度 |
|------|-----------|---------|
| SYS-GVG-01 | 报名阶段竞标规则与准入条件 | 报名逻辑 |
| SYS-GVG-02 | 多阶段流程（报名/备战/占城/结算）时间节点准确 | 阶段切换 |
| SYS-GVG-03 | 雕像摧毁/5秒重生/积分增减逻辑 | 雕像机制 |
| SYS-GVG-04 | 攻城BOSS刷新与击杀归属 | BOSS机制 |
| SYS-GVG-05 | 营地复活点管理与复活冷却 | 复活系统 |
| SYS-GVG-06 | 公会人数差异的血量补偿 | 平衡性 |
| SYS-GVG-07 | 完整占领后攻城BOSS刷新条件 | 占领规则 |
| SYS-GVG-08 | 断线重连在GVG中的表现 | 断线恢复 |
| SYS-GVG-09 | GVG结算奖励（个人/公会/赛季积分） | 奖励发放 |
| SYS-GVG-10 | 跨服GVG的数据同步与匹配 | 跨服扩展 |

**着重测试点：**
- GVG中断线重连：直接影响战局，重连后需无缝回归
- 雕像5秒重生逻辑：重生期间积分归属切换是竞态高发点
- GVG期间服务器负载：百人规模团战是压力峰值

**异常测试点：**
- 备战→占城切换瞬间的操作是否正确处理
- 雕像摧毁瞬间断线 → 积分归属判定
- GVG中服务器崩溃 → 是否支持战斗恢复/重赛
- 攻城BOSS被多公会同时击杀 → 归属判定
- 复活点被占领 → 是否导致无法复活的死局
- 公会人数悬殊 → 血量补偿是否足够

## 传统专项测试维度

> 以下4大专项测试适用于所有游戏系统。生成用例时根据系统实际情况，判断各专项的适用场景后设计对应用例。

### 专项1：性能测试

| 编号 | 测试考虑点 | 基准指标 |
|------|-----------|---------|
| PERF-01 | FPS稳定性 | 目标60帧，波动≤5帧 |
| PERF-02 | CPU占用率 | ≤60% |
| PERF-03 | GPU占用率 | ≤70% |
| PERF-04 | 内存占用 | 安卓≤512MB / iOS≤400MB |
| PERF-05 | DrawCall数量 | <100(优秀) / <200(良好) |
| PERF-06 | Batches数量 | <150(优秀) / <250(良好) / <400(可接受) |
| PERF-07 | Triangles数量 | <100K(优秀) / <200K(良好) / <500K(可接受) |
| PERF-08 | 加载时长 | 冷启动≤5s / 场景切换≤3s |
| PERF-09 | 发热耗电 | 持续30分钟温升≤8℃ / 耗电≤15% |
| PERF-10 | 网络延迟 | ≤100ms / 丢包≤1% |
| PERF-11 | 包体大小 | 首包≤500MB / 总包≤3GB（按项目定制） |

**着重测试点：**
- 大地图/战斗场景FPS：千人同屏是性能极限场景，低于30帧严重影响操作
- 内存泄漏：长时间游戏（24-72h）内存持续增长最终OOM闪退
- DrawCall/Batches：>200直接导致低端机掉帧卡顿

**异常测试点：**
- 长时间挂机（24h+）→ 内存/CPU持续攀升
- 大量特效同时播放（GVG/攻城）→ DrawCall飙升
- 频繁场景切换循环 → 内存不释放
- 低端机极限场景（2GB内存）→ 能否正常进入战斗
- 发热降频后帧率断崖式下跌

### 专项2：兼容测试

| 编号 | 测试考虑点 | 覆盖范围 |
|------|-----------|---------|
| COMPAT-01 | 机型兼容（高中低端） | 旗舰/高端/中端/低端/超低端5档，每档2-3台 |
| COMPAT-02 | 系统版本兼容 | Android 8-14 / iOS 13-17 / HarmonyOS 3-4 |
| COMPAT-03 | 分辨率适配 | 720P-4K；5套DPI资源 |
| COMPAT-04 | 刘海屏/挖孔屏适配 | SafeArea动态调整UI |
| COMPAT-05 | 折叠屏适配 | 展开/折叠切换+布局动态重组 |
| COMPAT-06 | 横竖屏切换 | 无黑屏/数据不丢失/布局自适应 |
| COMPAT-07 | 品牌ROM兼容 | MIUI/EMUI/ColorOS/One UI差异 |
| COMPAT-08 | GPU兼容性 | Mali/Adreno/PowerVR对OpenGL/Metal/Vulkan差异 |
| COMPAT-09 | 高刷新率适配 | 90Hz/120Hz避免动画卡顿 |
| COMPAT-10 | 输入适配 | 多点触控/全面屏手势冲突/陀螺仪差异 |

**通过标准：**
- 适配不通过机型用户占比 ≤1%
- 适配不通过机型数量占比 ≤3%
- Crash率 ≤1%

**异常测试点：**
- 折叠屏铰链中间态显示异常
- 全面屏手势冲突（底部上滑与操作区域重叠）
- Mali GPU渲染异常（特定Shader花屏/黑块）
- 横竖屏切换瞬间操作未保存
- 深色模式下UI不可用

### 专项3：安全测试

| 编号 | 测试考虑点 | 攻击/威胁类型 |
|------|-----------|-------------|
| SEC-01 | 协议加密强度（AES-256-GCM/ChaCha20/动态密钥） | 协议破解/MITM |
| SEC-02 | 内存修改防护（XOR加密/多存储副本+校验和/VMP） | 内存篡改 |
| SEC-03 | 加速器防护（服务端速度校验/时间窗口判定） | 速度篡改 |
| SEC-04 | 脱机/挂机防护（行为画像/操作随机性检测） | 自动化脚本 |
| SEC-05 | 数据校验（服务端二次确认/范围验证/时间验证） | 客户端篡改 |
| SEC-06 | SQL注入防护（参数化查询/ORM/输入校验） | SQL注入 |
| SEC-07 | 重放攻击防护（幂等键/nonce/时间窗口） | 重放攻击 |
| SEC-08 | 客户端加固（So/Dll加密/代码混淆/反调试） | 逆向工程 |
| SEC-09 | 物品复制漏洞（Duping）检测 | 复制漏洞 |
| SEC-10 | 路径遍历/SSRF/命令注入等Web安全漏洞 | 注入攻击 |

**着重测试点：**
- 服务端权威验证：所有关键操作必须服务端验证，客户端是"明牌"
- 协议加密与动态对抗：固定密钥/格式最易被破解
- 内存修改防护：GG修改器/Cheat Engine是最常见的入门级攻击
- 奖励/交易幂等性：重放攻击是最常见的后端攻击

**异常测试点：**
- GG修改器内存搜索修改关键数值
- 加速器2x/4x/8x倍速
- 封包篡改/伪造战斗结果包/领奖包
- 角色名/公会名/搜索框SQL注入
- 利用网络延迟复制物品
- 虚拟机/云手机绕过检测

### 专项4：压力/弱网测试

| 编号 | 测试考虑点 | 方法/标准 |
|------|-----------|---------|
| STRESS-01 | 并发用户测试 | 阶梯加压/突发加压至目标并发数 |
| STRESS-02 | 持续负载测试 | 24-72h持续运行监控资源泄漏 |
| STRESS-03 | 峰值波谷测试 | 模拟开服/活动开启瞬时峰值 |
| STRESS-04 | 故障恢复测试 | 模拟宕机后优雅降级与恢复 |
| STRESS-05 | 网络压力测试 | 模拟不同延迟/丢包/抖动 |
| STRESS-06 | 弱网2G/3G/4G/5G覆盖 | 各网络制式功能可用性 |
| STRESS-07 | 上下行100%丢包 | 极端场景不崩溃不丢数据 |
| STRESS-08 | 断线重连 | 重连后状态/位置/Buff一致性 |
| STRESS-09 | 网络切换 | WiFi↔4G/5G无缝切换 |
| STRESS-10 | 高延迟+高抖动 | 500ms+延迟±200ms抖动下操作容错 |

**弱网测试铁律：**
- 不能出现收支不等
- 不能卡死崩溃
- 核心功能不能受阻
- 不能损害玩家利益或额外获利
- 必须有断线重连机制

**着重测试点：**
- 开服/活动峰值并发：登录死锁是典型故障
- 弱网下收支不等：最严重的后果
- 断线重连session健壮性：session不健壮是典型bug
- PVP全量广播优化：需增量同步

**异常测试点：**
- 万人同时登录 → 死锁/超时
- GVG百人同时释放技能 → 带宽打满
- 弱网下领奖/交易 → 重复执行/丢失
- 完全断网时不崩溃，恢复后正确重连
- 频繁断线重连 → session堆积/资源泄漏

## 附录：测试指标速查表

### 性能指标基准

| 指标 | 优秀 | 良好 | 可接受 |
|------|------|------|--------|
| FPS | 60(波动≤5) | 45-60 | 30-45 |
| CPU | ≤40% | ≤60% | ≤80% |
| GPU | ≤50% | ≤70% | ≤85% |
| 内存(安卓) | ≤300MB | ≤512MB | ≤1GB |
| 内存(iOS) | ≤250MB | ≤400MB | ≤800MB |
| DrawCall | <100 | <200 | <400 |
| Batches | <150 | <250 | <400 |
| Triangles | <100K | <200K | <500K |

### 兼容测试通过标准

| 指标 | 通过标准 |
|------|---------|
| 适配不通过机型用户占比 | ≤1% |
| 适配不通过机型数量占比 | ≤3% |
| Crash率 | ≤1% |

### 安全威胁分布（行业参考）

| 威胁类型 | 占比 |
|---------|------|
| 破解 | 42% |
| 修改器 | 26% |
| 注入挂 | 15% |
| 加速器 | 9% |

---

> 本内容由 Coze AI 生成，请遵循相关法律法规及《人工智能生成合成内容标识办法》使用与传播。
